package ejercicios;

import sun.rmi.transport.StreamRemoteCall;

import java.util.List;

public class Tata {
    /*Emplyees oblect
using streams TO convert into a MAP map<employeeId, employeeObject>
    * */
/*
    List<String> employeesList;
    map<employeeId, employeeObject> employeesMap =  employeesList.stream().map(employee -> {
        return new Employee(employee);
    }).collect(Collectors.toMap(employee -> employee.getId(), employee -> employee));

*/

}
